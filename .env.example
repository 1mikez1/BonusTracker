# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Frontend variables (exposed to browser)
# Get these from: https://app.supabase.com > Your Project > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here

# Backend variables (server-side only, NEVER expose in frontend)
# Service Role Key has full database access - keep it secret!
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: CORS configuration for Edge Functions
# Comma-separated list of allowed origins (e.g., "https://example.com,https://app.example.com")
# Leave unset to allow all origins (*) - not recommended for production
# ALLOWED_ORIGINS=https://yourdomain.com
