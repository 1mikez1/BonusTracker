# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
*.log

# Next.js
.next
out
.vercel

# Environment variables
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea
.cursor/
*.swp
*.swo
*~

# Python virtual environment
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Build outputs
dist
build
*.tsbuildinfo

# Lock files (keep one, ignore others)
pnpm-lock.yaml
yarn.lock
package-lock.json

# Temporary files
*.tmp
*.temp
.cache

# Audit and analysis files (temporary, not for repository)
*_AUDIT.json
*_ANALYSIS.json
*_FIXES_SUMMARY.md
SECURITY_*.json
SENSITIVE_*.json
APPLICATION_*.json

# Data files (contain sensitive PII - migration scripts only, not needed in repo)
# Note: CSV files are needed locally for running migration scripts, but should NOT be committed
# Users should place their own CSV files in Data/ directory for migrations
Data/
mnt/
# Allow example/template CSV files (if you create them)
# !Data/example-*.csv
# !Data/*-template.csv
# But ignore all other CSV/Excel files
*.csv
*.xlsx
*.xls
GIT_HISTORY_CLEANUP.md
